#include "main.h"
/**
 * _strcat - entry point
 * @dest: string 1
 * @src: string 2
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
	char str1 = dest;
	char str2 = src;
	char total;

	total = strcat(str1, atr2);

	return total;
}
