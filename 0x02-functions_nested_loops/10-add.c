#include "main.h"
/**
 * add - a and b
 * Description - that adds two integers (a and b) returns the result
 * @a: interger 1
 * @b: interger 2
 * Return: sum
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
