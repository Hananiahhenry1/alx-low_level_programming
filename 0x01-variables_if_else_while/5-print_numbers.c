#include <stdio.h>
/**
 * main - Entry point
 *
 * Description - prints all single digit numbers of base
 *
 * Return: Always 0(success)
 */
int main(void)
{
	int n = 0;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
